export type Feature = {
    id: number;
    icon?: JSX.Element;
    title: string;
    paragraph: string;
};

export type Member = {
    id: number;
    img?: JSX.Element;
    title: string;
    paragraph: string;
}